# === Godot 4 ===
# Import cache + editor-generated data (regenerated on open via --import).
.godot/

# Legacy import folder (Godot 3) - harmless to keep ignored.
.import/

# Export config (can hold keystore passwords / signing secrets) and build output.
export.cfg
export_presets.cfg
*.pck
*.zip
*.apk
*.aab
*.exe
build/
exports/

# Auto-generated translations.
*.translation

# Mono / C# (not used here, kept for safety).
.mono/
data_*/
mono_crash.*.json

# === OS / editor junk ===
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*~
*.tmp
*.bak
